# Audiobookshelf — Storage paths
DATA_ROOT=../service_data/data/audiobookshelf

# Timezone
TZ=Asia/Kolkata

# Remaining Audiobookshelf env vars (image defaults — not previously set, no behavior change)
PORT=80
CONFIG_PATH=/config
METADATA_PATH=/metadata
