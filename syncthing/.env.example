# Syncthing — Storage paths
DATA_ROOT=../service_data/data/syncthing

# Timezone
TZ=Asia/Kolkata

# Remaining Syncthing env vars (linuxserver.io base image default — not previously set, no behavior change)
UMASK=022
