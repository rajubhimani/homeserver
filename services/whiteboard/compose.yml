services:
  whiteboard:
    # Was floating on :stable (no clean numbered release tags published
    # upstream at the time this service was first added -- GHCR listing was
    # a mix of beta/daily/commit-hash tags). Numbered tags exist now --
    # pinned to v1.5.9, confirmed via matching image digest to be the exact
    # same image :stable already resolved to (no behavior change from this
    # pin, just removes the float). Matches the Nextcloud "whiteboard" app
    # version exactly -- app and backend are versioned together.
    image: ghcr.io/nextcloud-releases/whiteboard:v1.5.9
    container_name: whiteboard
    restart: unless-stopped
    env_file: .env
    environment:
      # Internal container URL, not the public domain — avoids a hairpin
      # through cloudflared for every file read/write, same reasoning as
      # ONLYOFFICE's "Advanced server settings" internal address.
      NEXTCLOUD_URL: http://nextcloud
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3002/status"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s
    networks:
      - homeserver

networks:
  homeserver:
    external: true
