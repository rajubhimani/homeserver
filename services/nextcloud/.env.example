# Nextcloud — Storage paths
DATA_ROOT=../../service_data/data/nextcloud
USER_DATA_ROOT=/mnt/seagate
OS_ISO_ROOT=/mnt/os-iso

# Postgres
POSTGRES_DB=nextcloud
POSTGRES_USER=nextcloud
POSTGRES_PASSWORD=your_strong_password

# Nextcloud admin account
# IMPORTANT: password must not contain $ (dollar sign) — Docker Compose will
# treat $VAR patterns as variable references and mangle the value. Use hex or
# alphanumeric passwords only (e.g. openssl rand -hex 20)
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=your_strong_password_no_dollar_signs

# How often nextcloud-cron rescans OS_ISO_ROOT (occ files:scan --all), in
# seconds. Default 300 (5 minutes) is fine for most setups. Raise this if
# OS_ISO_ROOT sits on slow storage or a disk shared with another service's
# heavy I/O — see docs/services/jellyfin.md's playback-buffering entry for
# the general pattern.
CRON_SCAN_INTERVAL_SECONDS=300

# Remaining Nextcloud env vars (image defaults — not previously set, no behavior change)
NEXTCLOUD_DATA_DIR=/var/www/html/data
PHP_MEMORY_LIMIT=512M
PHP_UPLOAD_LIMIT=512M
APACHE_BODY_LIMIT=1073741824
NEXTCLOUD_UPDATE=0

