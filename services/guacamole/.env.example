# Guacamole — Storage paths
DATA_ROOT=../../service_data/data/guacamole

# Postgres
POSTGRES_DB=guacamole
POSTGRES_USER=guacamole
POSTGRES_PASSWORD=your_strong_password
# Note: the guacamole webapp/guacd containers don't use env_file, so their
# own no-op documentation vars live in compose.yml's environment blocks
# instead of here — see compose.yml.
