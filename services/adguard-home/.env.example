# AdGuard Home — Storage paths
DATA_ROOT=../../service_data/data/adguard-home

# No other env vars — all configuration (DNS upstreams, filter lists, admin
# password) is set through the first-run web setup wizard and stored in
# conf/AdGuardHome.yaml, not env vars.
