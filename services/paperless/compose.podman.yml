services:
  paperless-db:
    security_opt:
      - label=disable
  paperless-redis:
    security_opt:
      - label=disable
  paperless:
    security_opt:
      - label=disable
    depends_on:
      - paperless-db
      - paperless-redis
