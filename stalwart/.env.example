# Stalwart Mail Server — Storage paths
DATA_ROOT=../service_data/stalwart

# Admin recovery credentials (set before first start)
# After setup wizard completes, manage users via the web admin
STALWART_RECOVERY_ADMIN=admin:your_strong_password
