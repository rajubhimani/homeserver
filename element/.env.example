# Element Web has no persistent server-side data (all state lives in the
# browser) and no configurable env vars of its own — DOMAIN is injected
# automatically by homeserver.py. No .env needed to run, kept for consistency.
