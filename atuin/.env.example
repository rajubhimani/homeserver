# Atuin sync server — Storage paths
DATA_ROOT=../service_data/data/atuin

# Postgres
POSTGRES_DB=atuin
POSTGRES_USER=atuin
POSTGRES_PASSWORD=your_strong_password

# Self-registration for new atuin CLI clients
ATUIN_OPEN_REGISTRATION=true

# Remaining Atuin env vars (image defaults — not previously set, no behavior change)
RUST_LOG=info,atuin_server=debug
