services:
  nextcloud-db:
    image: postgres:18
    container_name: nextcloud-db
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ${DATA_ROOT}/postgres-nextcloud:/var/lib/postgresql
      - ./postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - homeserver

  nextcloud:
    image: nextcloud:latest
    container_name: nextcloud
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_HOST: nextcloud-db
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      NEXTCLOUD_ADMIN_USER: ${NEXTCLOUD_ADMIN_USER}
      NEXTCLOUD_ADMIN_PASSWORD: ${NEXTCLOUD_ADMIN_PASSWORD}
      NEXTCLOUD_TRUSTED_DOMAINS: ${NEXTCLOUD_TRUSTED_DOMAINS}
      NEXTCLOUD_TRUSTED_PROXIES: nginx-proxy-manager
    volumes:
      - ${DATA_ROOT}/nextcloud/config:/var/www/html/config
      - ${DATA_ROOT}/nextcloud/data:/var/www/html/data
      - ${DATA_ROOT}/nextcloud/custom_apps:/var/www/html/custom_apps
      - ${DATA_ROOT}/nextcloud/version.php:/var/www/html/version.php
      - ./hooks/before-starting:/docker-entrypoint-hooks.d/before-starting
      - ${USER_DATA_ROOT}:/mnt/seagate
    depends_on:
      nextcloud-db:
        condition: service_healthy
    networks:
      - homeserver

networks:
  homeserver:
    external: true