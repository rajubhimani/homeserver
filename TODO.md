# TODO

Deferred follow-ups — things deliberately left undone with a plan to revisit, not bugs or in-progress work. Check items off or delete the line once handled; add a one-line "why deferred" note so it's still meaningful months later.

- [ ] **CrowdSec: wire up a bouncer for actual IP blocking.** Currently detection-only — reads nginx-plain's logs via the Docker log API and builds ban decisions, but nothing blocks anything yet. The standard bouncer (iptables/nftables-based) doesn't fit this host (Windows/Docker Desktop, no native iptables). Options to revisit: (a) rebuild nginx-plain on OpenResty and use the official CrowdSec nginx Lua bouncer module for proxy-layer blocking, or (b) re-evaluate once/if the stack ever moves to a native Linux host, where the standard firewall bouncer would just work. See `docs/services/crowdsec.md`.
- [ ] **PostHog: not added — deferred, self-hosting is much heavier than expected.** The real deployment is ~30-40 containers (Temporal, Kafka, ClickHouse, MinIO, several Rust microservices) installed via a bash script that clones PostHog's whole repo and manages its own Caddy/TLS, not a drop-in compose file. Plausible was added instead for core self-hosted analytics. Full findings and revisit options: `docs/research/posthog-self-hosting.md`.
- [ ] **Dub: not added — hard blocker, not just heavy.** Dub's own self-hosting docs explicitly say "Docker is currently not supported." It's built assuming Vercel hosting + GitHub OAuth + Tinybird (hosted ClickHouse) + Upstash Redis + PlanetScale MySQL + Cloudflare R2, all via REST APIs to external SaaS — no local-service fallback exists yet (the project has stated intent to add one, not a timeline). Re-check `https://dub.co/docs/self-hosting` periodically; revisit once/if a genuine Docker path exists.
- [ ] **Coolify: pinned to `:edge` instead of a stable tag.** `coollabsio/coolify` has no semver-tagged releases on Docker Hub right now (an apparent gap in their release pipeline) — only `edge`/`next`/feature-branch tags exist. Check `https://hub.docker.com/r/coollabsio/coolify/tags` periodically and switch `coolify/compose.yml` to a real version tag the moment one is published. See `docs/services/coolify.md`.
