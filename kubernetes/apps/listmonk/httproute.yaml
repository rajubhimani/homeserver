apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: listmonk
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "listmonk.k8s.local"
  rules:
    - backendRefs:
        - name: listmonk
          port: 9000
