apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: calcom
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "calcom.k8s.local"
  rules:
    - backendRefs:
        - name: calcom
          port: 3000
