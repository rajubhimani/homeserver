apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: brave
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "brave.k8s.local"
  rules:
    - backendRefs:
        - name: brave
          port: 3000
