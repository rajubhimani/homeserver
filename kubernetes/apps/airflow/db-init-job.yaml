# Onboards airflow onto the shared Postgres server (kubernetes/cluster/
# postgres/) instead of a dedicated StatefulSet — copied from
# kubernetes/cluster/postgres/db-init-job.template.yaml with SERVICE_NAME
# replaced. Airflow has no version/extension requirement that needs its
# own Postgres.
apiVersion: batch/v1
kind: Job
metadata:
  name: airflow-db-init
  namespace: data
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: db-init
          image: postgres:18.6-alpine
          env:
            - name: PGHOST
              value: postgres-shared.data.svc.cluster.local
            - name: PGUSER
              value: postgres
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-shared-credentials
                  key: postgres-password
            - name: SERVICE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: airflow-db-credentials
                  key: password
          command:
            - sh
            - -c
            - |
              set -e
              psql -tc "SELECT 1 FROM pg_roles WHERE rolname='airflow'" | grep -q 1 || \
                psql -c "CREATE ROLE \"airflow\" LOGIN PASSWORD '$SERVICE_DB_PASSWORD';"
              psql -tc "SELECT 1 FROM pg_database WHERE datname='airflow'" | grep -q 1 || \
                psql -c "CREATE DATABASE \"airflow\" OWNER \"airflow\";"
