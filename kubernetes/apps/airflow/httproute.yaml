apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: airflow
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "airflow.k8s.local"
  rules:
    - backendRefs:
        - name: airflow-apiserver
          port: 8080
