apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chromium
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "chromium.k8s.local"
  rules:
    - backendRefs:
        - name: chromium
          port: 3000
