apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "jellyfin.k8s.local"
  rules:
    - backendRefs:
        - name: jellyfin
          port: 8096
