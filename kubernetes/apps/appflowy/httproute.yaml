apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: appflowy
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "appflowy.k8s.local"
  rules:
    - backendRefs:
        - name: appflowy-nginx
          port: 80
