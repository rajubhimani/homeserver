apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: documenso-db
  namespace: apps
spec:
  serviceName: documenso-db
  replicas: 1
  selector:
    matchLabels:
      app: documenso-db
  template:
    metadata:
      labels:
        app: documenso-db
    spec:
      enableServiceLinks: false
      containers:
        - name: postgres
          image: postgres:18.4-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: documenso
            - name: POSTGRES_USER
              value: documenso
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: documenso-db-credentials
                  key: password
          args:
            - -c
            - shared_buffers=128MB
            - -c
            - max_connections=20
            - -c
            - work_mem=4MB
            - -c
            - maintenance_work_mem=64MB
            - -c
            - effective_cache_size=256MB
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          resources:
            limits:
              memory: 384Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "documenso"]
            initialDelaySeconds: 5
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: documenso-db
  namespace: apps
spec:
  clusterIP: None
  selector:
    app: documenso-db
  ports:
    - port: 5432
