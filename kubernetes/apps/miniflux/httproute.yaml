apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: miniflux
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "miniflux.k8s.local"
  rules:
    - backendRefs:
        - name: miniflux
          port: 8080
