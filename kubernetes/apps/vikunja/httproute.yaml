apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vikunja
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "vikunja.k8s.local"
  rules:
    - backendRefs:
        - name: vikunja
          port: 3456
