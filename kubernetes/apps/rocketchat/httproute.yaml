apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rocketchat
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "rocketchat.k8s.local"
  rules:
    - backendRefs:
        - name: rocketchat
          port: 3000
