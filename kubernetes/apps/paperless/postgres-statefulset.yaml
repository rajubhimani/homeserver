# Own dedicated Postgres — same init-script-owns-schema pattern as
# mealie/guacamole/forgejo/firefly. Resource flags copied verbatim from
# paperless/compose.yml's paperless-db `command:` — here as `args:`, not
# `command:` (Kubernetes' command: overrides ENTRYPOINT itself and skips the
# postgres image's own entrypoint wrapper that drops root; see
# TROUBLESHOOTING.md's "command: means different things in Compose vs
# Kubernetes").
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: paperless-db
  namespace: apps
spec:
  serviceName: paperless-db
  replicas: 1
  selector:
    matchLabels:
      app: paperless-db
  template:
    metadata:
      labels:
        app: paperless-db
    spec:
      enableServiceLinks: false
      containers:
        - name: postgres
          image: postgres:18.6
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: paperless
            - name: POSTGRES_USER
              value: paperless
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: paperless-db-credentials
                  key: password
          args:
            - -c
            - shared_buffers=128MB
            - -c
            - max_connections=20
            - -c
            - work_mem=4MB
            - -c
            - maintenance_work_mem=64MB
            - -c
            - effective_cache_size=256MB
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
            - name: init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
          resources:
            limits:
              memory: 384Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "paperless"]
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: init
          configMap:
            name: paperless-postgres-init
            defaultMode: 0755
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: paperless-db
  namespace: apps
spec:
  clusterIP: None
  selector:
    app: paperless-db
  ports:
    - port: 5432
