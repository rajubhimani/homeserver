apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: photoprism
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "photoprism.k8s.local"
  rules:
    - backendRefs:
        - name: photoprism
          port: 2342
