apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plausible
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "plausible.k8s.local"
  rules:
    - backendRefs:
        - name: plausible
          port: 8000
