apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: silverbullet
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "silverbullet.k8s.local"
  rules:
    - backendRefs:
        - name: silverbullet
          port: 3000
