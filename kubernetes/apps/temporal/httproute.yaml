apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: temporal
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "temporal.k8s.local"
  rules:
    - backendRefs:
        - name: temporal-ui
          port: 8080
