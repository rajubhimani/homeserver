apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: open-webui
  namespace: apps
spec:
  parentRefs:
    - name: homeserver-gateway
      namespace: infra
  hostnames:
    - "open-webui.k8s.local"
  rules:
    - backendRefs:
        - name: open-webui
          port: 8080
