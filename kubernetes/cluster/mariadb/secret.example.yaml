# Template only — the real Secret is never committed (same convention as
# .env vs .env.example elsewhere in this repo). Create it with:
#
#   kubectl create secret generic mariadb-shared-credentials -n data \
#     --from-literal=root-password="$(openssl rand -hex 16)"
#
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-shared-credentials
  namespace: data
type: Opaque
stringData:
  root-password: changeme
