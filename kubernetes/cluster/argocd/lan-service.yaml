apiVersion: v1
kind: Service
metadata:
  name: argocd-server-lan
  namespace: argocd
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: argocd-server
  ports:
    - name: http
      port: 18081
      targetPort: 8080
