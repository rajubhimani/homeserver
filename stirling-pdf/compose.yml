services:
  stirling-pdf:
    image: stirlingtools/stirling-pdf:latest
    container_name: stirling-pdf
    restart: unless-stopped
    env_file: .env
    volumes:
      - ${DATA_ROOT}/stirling-pdf/configs:/configs
      - ${DATA_ROOT}/stirling-pdf/logs:/logs
      - ${DATA_ROOT}/stirling-pdf/customFiles:/customFiles
      - ${DATA_ROOT}/stirling-pdf/pipeline:/pipeline
      - ${DATA_ROOT}/stirling-pdf/tessdata:/usr/share/tessdata
    networks:
      - homeserver

networks:
  homeserver:
    external: true
