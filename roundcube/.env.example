# Roundcube — Storage paths
DATA_ROOT=../service_data/roundcube

# IMAP server (defaults to Stalwart on homeserver network)
IMAP_HOST=stalwart
IMAP_PORT=143

# SMTP server
SMTP_HOST=stalwart
SMTP_PORT=587
